# Brain Bot V16 Claude Rules

## Golden Rules

Never delete working code.

Never rewrite modules without inspection.

Always inspect architecture first.

Always inspect imports.

Never invent APIs.

Always preserve backwards compatibility.

Prefer additive changes.

Run tests before every commit.

Run Ruff before delivery.

Never remove documentation.

Never silently change business logic.

Explain architectural conflicts before coding.

Never rewrite Risk Engine blindly.

Never rewrite Execution Layer blindly.

Never rewrite Dashboard blindly.

Always update architecture.md.

Always update CHANGELOG.

Always produce unified diffs.

Never introduce duplicate managers.

Keep modules loosely coupled.

Preserve test coverage.

Do not hardcode secrets.

Never commit .env.

Never commit database files.

Use feature branches.

Document every new module.