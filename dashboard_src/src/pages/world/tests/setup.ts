// dashboard_src/src/pages/world/tests/setup.ts
// Vitest setup for the world/ page tests. Phase W10's test suite only
// exercises pure functions (sceneMapping.ts) and the fetch-based REST
// client (api.ts) — neither needs a real canvas/WebGL context, so this
// file stays intentionally minimal rather than carrying forward mocks
// (Web Audio, canvas 2D context, etc.) nothing here still needs.
export {}
